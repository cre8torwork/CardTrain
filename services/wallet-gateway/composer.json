{
  "name": "cardtrain/wallet-gateway",
  "description": "Card Train wallet gateway — submits Apple Pay / Google Pay authorizations to CyberSource via the Simple Order API (SOAP + P12). Called server-to-server by the Supabase edge functions.",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "ext-curl": "*",
    "ext-openssl": "*",
    "ext-soap": "*",
    "cybersource/sdk-php": "*"
  },
  "autoload": {
    "psr-4": { "CardTrain\\WalletGateway\\": "src/" }
  },
  "config": {
    "sort-packages": true
  }
}
